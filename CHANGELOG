v0.16.1
  Bugfix: Container is no more created when there is already one.

v0.16
  Question mark selector feature.
  Shout for [] and ? features when selector is an array.
  `new $.PageObject(opts)` feature
  Namespace option feature.

v0.15.1
  Fix for

v0.15
  priv.isElement => $.isElement
  string errors => new Error()
  priv.extractParts => $.extractParts
  $.isPageObject(po)
  $(po.DOM.container).data('pageObject', po)

v0.14
  One day…
